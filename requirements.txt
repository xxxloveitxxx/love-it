Flask==3.0.3
gunicorn==22.0.0
Jinja2==3.1.4
playwright==1.45.0
supabase==2.6.0
python-dotenv==1.0.1
gspread==6.0.2
google-auth==2.35.0


greenlet>=3.1.0
pandas>=2.2.3
# optionally pick a newer pandas like 2.3.x if you want
# AND make sure playwright does not pin an older greenlet version
